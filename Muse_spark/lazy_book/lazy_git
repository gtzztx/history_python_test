
git config --list                             #查看git所有配置
git config --global user.name "你的用户名"    #设置你的用户名
git config --global user.email "你的邮箱"     #设置你的邮箱
git remote add origin [GitHub 仓库地址]       #设置你的仓库地址并让它关联origin
git remote -v             #详细查看远程仓库的详细信息
git add [指定文件]        #指定更新对应的文件，全部更新直接 . 
git commit -m "备注信息"  #提交备注内容
git push                  #上传远程仓库
git push -u origin [具体位置]
git pull                  #将远程仓库里的最新文件拉到本地
Git log                   #查看之前提交的记录，每条只显示提交的时间、作者、备注信息
Git diff                  #查看未提交的修改具体区别


*git commit --amend --reset-author  #修改这次提交的作者信息

git config --global --unset https.proxy #取消全局代理
git config --global --get https.proxy   #检查当前代理
git config --global http.proxy [参数]    #设置当前代理

出现的报错

1. 443 SSL Operation timed out
先查看一下网络链接，判断是否可以连接到github，
ping github.com -c [次数]  #尝试链接网站
curl -I https://github.com #尝试访问网站

2.fatal: unable to access[网址]:Empty reply from server
先取消全局代理，重新尝试推送，
如果出现443报错，立即重新设置当前代理。

